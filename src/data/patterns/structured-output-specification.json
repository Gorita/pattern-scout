{
  "id": "structured-output-specification",
  "title": "Structured Output Specification",
  "title_ko": "구조화된 출력 명세",
  "category": "Reliability & Eval",
  "status": "established",
  "original_url": "https://vercel.com/blog/what-we-learned-building-agents-at-vercel",
  "problem": {
    "en": "Free-form agent outputs are difficult to validate, parse, and integrate with downstream systems. When agents return unstructured text, you face unpredictable output formats requiring complex parsing, difficult validation and error handling, brittle integration with automated workflows, inconsistent categorization and classification, and manual post-processing to extract structured data. This makes it nearly impossible to build reliable multi-step workflows where one agent's output feeds into another system or agent.",
    "ko": "자유 형식 에이전트 출력은 검증, 파싱, 다운스트림 시스템과의 통합이 어렵습니다. 에이전트가 비정형 텍스트를 반환하면 복잡한 파싱이 필요한 예측 불가능한 출력 형식, 어려운 검증 및 오류 처리, 자동화 워크플로우와의 취약한 통합, 일관성 없는 분류, 구조화된 데이터 추출을 위한 수동 후처리에 직면합니다. 이로 인해 한 에이전트의 출력이 다른 시스템이나 에이전트에 입력되는 신뢰할 수 있는 다단계 워크플로우를 구축하는 것이 거의 불가능해집니다."
  },
  "solution": {
    "en": "Constrain agent outputs using deterministic schemas that enforce structured, machine-readable results. Instead of allowing free-form text responses, specify exact output formats using type systems, JSON schemas, or framework-specific structured output APIs.\n\n**Core approach:**\n\n**Define explicit output schemas:**\n- Use TypeScript interfaces, JSON Schema, or Pydantic models\n- Specify required fields, types, and constraints\n- Define enumerations for categorical outputs\n- Document field semantics and validation rules\n\n**Leverage framework structured output APIs:**\n- OpenAI's structured outputs with JSON schema (constrained decoding)\n- Anthropic's tool use for structured results\n- Vercel AI SDK's `generateObject` function\n- LangChain's output parsers\n- LlamaIndex Pydantic programs\n- Instructor retry wrapper\n\n**Validate at generation time:**\n- Framework ensures LLM adheres to schema\n- Type errors caught before reaching application code\n- Guaranteed parseable outputs\n\n```typescript\nimport { generateObject } from 'ai';\nimport { z } from 'zod';\n\nconst LeadQualificationSchema = z.object({\n  qualification: z.enum(['qualified', 'unqualified', 'needs_review']),\n  confidence: z.number().min(0).max(1),\n  companySize: z.enum(['enterprise', 'mid-market', 'smb', 'unknown']),\n  estimatedBudget: z.string().optional(),\n  nextSteps: z.array(z.string()),\n  reasoning: z.string()\n});\n\nconst result = await generateObject({\n  model: openai('gpt-4'),\n  schema: LeadQualificationSchema,\n  prompt: `Analyze this lead: ${leadData}`\n});\n\nif (result.object.qualification === 'qualified') {\n  await sendToSalesTeam(result.object);\n}\n```",
    "ko": "결정론적 스키마를 사용하여 구조화된 기계 판독 가능한 결과를 강제하는 방식으로 에이전트 출력을 제한합니다. 자유 형식 텍스트 응답을 허용하는 대신, 타입 시스템, JSON 스키마 또는 프레임워크별 구조화된 출력 API를 사용하여 정확한 출력 형식을 지정합니다.\n\n핵심 접근법:\n\n명시적 출력 스키마를 정의합니다: TypeScript 인터페이스, JSON Schema 또는 Pydantic 모델을 사용하고, 필수 필드, 타입, 제약 조건을 지정하며, 범주형 출력에 열거형을 정의합니다.\n\n프레임워크 구조화 출력 API를 활용합니다: OpenAI의 JSON 스키마 구조화 출력, Anthropic의 도구 사용, Vercel AI SDK의 generateObject 함수, LangChain의 출력 파서 등을 사용합니다.\n\n생성 시점에 검증합니다: 프레임워크가 LLM의 스키마 준수를 보장하고, 타입 오류가 애플리케이션 코드에 도달하기 전에 포착되며, 파싱 가능한 출력이 보장됩니다."
  },
  "ascii_diagram": "┌─────────────┐\n│ Agent Input │\n└──────┬──────┘\n       │\n┌──────▼──────┐\n│ LLM +       │\n│ Schema      │\n└──────┬──────┘\n       │\n┌──────▼──────┐\n│ Validated   │\n│ Structured  │\n│ Output      │\n└──────┬──────┘\n       │\n   ┌───┴───┐\n   ▼       ▼\n [DB]   [API]",
  "mermaid_diagram": "flowchart TD\n    A[Agent Input] --> B[LLM + Schema]\n    B --> C[Validated Structured Output]\n    C --> D[Database Storage]\n    C --> E[API Integration]\n    C --> F[Next Agent Phase]",
  "code_example": "import { generateObject } from 'ai';\nimport { z } from 'zod';\n\nconst LeadSchema = z.object({\n  qualification: z.enum(['qualified', 'unqualified', 'review']),\n  confidence: z.number().min(0).max(1),\n  nextSteps: z.array(z.string()),\n  reasoning: z.string()\n});\n\nconst result = await generateObject({\n  model: openai('gpt-4'),\n  schema: LeadSchema,\n  prompt: `Analyze: ${leadData}`\n});",
  "when_to_use": {
    "en": ["Multi-phase agent workflows requiring structured handoffs", "Classification and categorization tasks", "Data extraction and transformation", "Integration with databases or APIs", "Compliance and audit requirements", "Quality assurance and validation"],
    "ko": ["구조화된 핸드오프가 필요한 다단계 에이전트 워크플로우", "분류 및 범주화 작업", "데이터 추출 및 변환", "데이터베이스 또는 API와의 통합", "컴플라이언스 및 감사 요구사항", "품질 보증 및 검증"]
  },
  "pros": {
    "en": ["Guaranteed parseable outputs eliminate parsing errors", "Compile-time type safety in typed languages", "Seamless integration to databases, APIs, workflows", "Built-in constraint enforcement for validation", "Schema validation prevents prompt injection before execution", "Explicit contracts between system components for maintainability", "Easy to verify output correctness for testability"],
    "ko": ["파싱 오류를 제거하는 보장된 파싱 가능 출력", "타입 언어에서의 컴파일 타임 타입 안전성", "데이터베이스, API, 워크플로우와의 원활한 통합", "검증을 위한 내장 제약 조건 강제", "실행 전 프롬프트 인젝션을 방지하는 스키마 검증", "유지보수성을 위한 시스템 컴포넌트 간 명시적 계약", "테스트 용이성을 위한 출력 정확성 검증 용이"]
  },
  "cons": {
    "en": ["Schema changes require coordinated updates", "Requires upfront schema design effort", "May constrain useful free-form outputs", "Relies on LLM provider schema support", "Too strict schemas may cause generation failures", "Adapting schemas as requirements change creates friction"],
    "ko": ["스키마 변경 시 조율된 업데이트 필요", "사전 스키마 설계 노력 필요", "유용한 자유 형식 출력을 제한할 수 있음", "LLM 제공자의 스키마 지원에 의존", "너무 엄격한 스키마가 생성 실패를 유발할 수 있음", "요구사항 변경에 따른 스키마 적응 시 마찰 발생"]
  },
  "tags": ["structured-output", "schema", "validation", "reliability"]
}
